import { ComingSoon } from '../_stub';

export default function Page() {
  return (
    <ComingSoon
      title="Local Flights"
      description="Manage flights stored in your local database (separate from Duffel real-time inventory)."
    />
  );
}
